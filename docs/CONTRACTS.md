# Contracts

Core domain: workflow state/scheduling/task execution/recovery.

API, domain, provider, event and configuration contracts are explicit boundary artifacts. Compatibility changes require tests and an ADR.